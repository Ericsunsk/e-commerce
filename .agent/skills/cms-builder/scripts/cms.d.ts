/**
 * DEPRECATED: This file is no longer used for type definitions.
 *
 * We have migrated to using `pocketbase-typegen` for generating TypeScript definitions
 * directly from the PocketBase schema.
 *
 * Please run the type generation command (e.g., `npx pocketbase-typegen ...`)
 * to generate `src/lib/pocketbase-types.ts`.
 */

export {};

/**
 * Payment Domain (Client Contract)
 *
 * Server-only context (no components, no `ui/`). Exposes the browser-side
 * client for its own admin settings endpoints.
 */

export * from './infrastructure/payment-settings-client.client';
